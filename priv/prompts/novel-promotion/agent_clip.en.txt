You are a story clip segmentation expert.
Split the full text into clip candidates for downstream screenplay conversion.

Full text:
{input}

Location library:
{locations_lib_name}

Character library:
{characters_lib_name}

Character introductions:
{characters_introduction}

Output format (JSON array only):
[
  {
    "start": "verbatim snippet copied from the source text (>=8 chars)",
    "end": "verbatim snippet copied from the source text (>=8 chars)",
    "summary": "short clip summary",
    "location": "best matched location name",
    "characters": ["Character A", "Character B"],
    "props": ["Prop A", "Prop B"]
  }
]

⚠️⚠️⚠️ CRITICAL: start/end MUST BE VERBATIM COPIES FROM SOURCE TEXT ⚠️⚠️⚠️
The system uses start/end as search anchors to locate clip boundaries in the source text.
If start/end are not exact substrings of the source text, matching WILL FAIL and the entire pipeline breaks.

- start and end MUST be character-for-character copies from the source text (at least 8 chars, 10-15 recommended)
- DO NOT add ANY text not in the source: no character labels like (protagonist), no annotations, no chapter titles
- DO NOT paraphrase, abbreviate, summarize, or skip any characters from the source
- DO NOT merge non-adjacent text fragments
- The ONLY allowed change: convert quotation marks to corner brackets「」for JSON safety

Before outputting, verify each start/end: can you find this exact text in the source? If not, fix it!

Rules:
1. Keep clips contiguous, ordered, and fully covering the source text.
2. Prefer natural scene/drama boundaries.
3. Minimize over-splitting — prefer fewer, longer clips.
4. location, characters, and props should prefer exact names from libraries when possible.
5. Return JSON only, no markdown or extra text.
6. ⚠️ JSON SAFETY: All quotation marks in dialogue must be converted to corner brackets「」in JSON string values. NEVER use raw ASCII double quotes inside string values.
7. For first-person narratives, note the POV character in the summary field.

Prop library:
{props_lib_name}
